--- a/apps/evidia-mobile/android/app/src/main/java/ai/evidia/mobile/services/AuthViewModel.kt
+++ b/apps/evidia-mobile/android/app/src/main/java/ai/evidia/mobile/services/AuthViewModel.kt
@@ -18,7 +18,6 @@
 import ai.evidia.mobile.core.UserInfo
 import ai.evidia.mobile.core.TenantSummary
 import uniffi.evidia_api_contract.DeviceRegistrationRequest
-import uniffi.evidia_api_contract.CompleteOnboardingRequest
 import android.os.Build
 import okhttp3.OkHttpClient
 import okhttp3.Request
@@ -492,12 +491,16 @@
         viewModelScope.launch {
             _isLoading.value = true
             try {
-                // Get current onboarding state to fetch version
-                val onboardingState = core.getOnboardingState(sessionToken)
+                // Onboarding is already completed by the onboarding screen flow.
+                // Refresh user info and finalize local authenticated state.
+                val updatedUserInfo = core.getCurrentUser(sessionToken)
+                _userInfo.value = updatedUserInfo
 
-                // Mark onboarding as complete
-                val request = CompleteOnboardingRequest(expectedVersion = onboardingState.version)
-                core.completeOnboarding(sessionToken, request)
+                if (updatedUserInfo.needsOnboarding) {
+                    _state.value = AuthState.NeedsOnboarding
+                    _errorMessage.value = "Onboarding completion is still pending. Please try again."
+                    return@launch
+                }
 
                 // Update local state
                 _state.value = AuthState.Authenticated
--- a/apps/evidia-mobile/ios/EvidiaMobile/EvidiaMobile/Services/AuthManager.swift
+++ b/apps/evidia-mobile/ios/EvidiaMobile/EvidiaMobile/Services/AuthManager.swift
@@ -452,14 +452,16 @@
     defer { isLoading = false }
 
     do {
-      // Get current onboarding state to fetch version
-      let onboardingState = try await core.getOnboardingState(sessionToken: sessionToken)
+      // Onboarding is already completed by the onboarding view flow.
+      // Refresh user info and finalize local authenticated state.
+      let updatedUserInfo = try await core.getCurrentUser(sessionToken: sessionToken)
+      userInfo = updatedUserInfo
 
-      // Mark onboarding as complete
-      _ = try await core.completeOnboarding(
-        sessionToken: sessionToken,
-        request: CompleteOnboardingRequest(expectedVersion: onboardingState.version)
-      )
+      if updatedUserInfo.needsOnboarding {
+        state = .needsOnboarding
+        errorMessage = "Onboarding completion is still pending. Please try again."
+        return
+      }
 
       // Update local state
       state = .authenticated
